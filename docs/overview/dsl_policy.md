# DSL Policy

- Gherkin (`.feature`) is the only DSL for rule specifications.
- YAML rule files are legacy artifacts and will be archived after migration.
- Normalization logic is documented in `docs/overview/normalization_catalog.md` and is not a DSL.
