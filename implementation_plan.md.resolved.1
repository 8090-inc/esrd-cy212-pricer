# Implementation Plan: COBOL Pricer Reverse Engineering

## Goal Description
Reverse engineer the ESRD CY21.2 Mainframe Pricer COBOL codebase to create four key deliverables in three milestones (V1, V2, V3). The deliverables are:
1. Human-Readable Rule Book
2. Machine-Readable Rule Artifacts
3. System Architecture Documentation (UML/Sequence)
4. Rule to Manual Linkage (Source Code Provenance)

## Deliverables & Approach

### 1. Human-Readable Rule Book
- **Goal:** Identify, analyze, and normalize legacy rules/edits.
- **V1 Focus:** Identify rule boundaries in [ESDRV212](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/ESDRV212) (Driver) and the initialization/validation setup in [ESCAL212](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/ESCAL212) (Calculation).
- **Format:** Markdown document detailing:
  - **Rule Taxonomy:** Categorization of edits (e.g., Input Validation, Date Routing, Base Rate Calculation).
  - **Conditions/Actions:** The `IF/ELSE` conditions translated into natural language business rules.
  - **Call Graphs & Control Flow:** Visual text narratives of how control is passed (e.g., Driver -> Date Check -> Specific Year Calculator).
  - **Data Definitions:** Mapping of variables from interface copybooks (e.g., [BILLCPY](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/BILLCPY), [RTCCPY](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/RTCCPY)).

### 2. Machine-Readable Rule Artifacts
- **Goal:** Model and export rules in machine-readable formats with test harnesses and tooling.
- **V1 Focus:** Establish the schema and tooling baseline.
- **Format & Tooling:**
  - **Schema:** JSON Schema representing a normalized rule structure (ID, Condition, Action, Variables Used).
  - **Artifacts:** JSON files containing the extracted rules for V1.
  - **Test Harness:** A python script (`rule_validator.py`) to ingest the JSON rules and validate them against the schema. This acts as the baseline for the import test harness to validate rule integrity.
  - **Discovery Environment:** Utilizing the local file system and standard CLI tools (grep, Python) to ensure repeatable reruns.

### 3. System Architecture Documentation
- **Goal:** UML Asciidraw diagrams and sequence diagrams representing the system.
- **V1 Focus:** The interaction between the macro components.
- **Format:** Markdown files utilizing Mermaid.js for embedding UML.
  - **Structural (Component Diagram):** Showing `DRIVER`, `CALCULATION MODULES`, and `COPYBOOKS` datasets.
  - **Behavioral (Sequence Diagram):** Showing the temporal flow from external invocation -> [ESDRV212](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/ESDRV212) -> [ESCAL212](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/ESCAL212) -> Response.

### 4. Rule to Manual Linkage (Traceability)
- **Goal:** Source code provenance and SME validation workflows.
- **V1 Focus:** Establishing the Traceability Matrix format tying the English rules to COBOL line numbers.
- **Format:** Markdown Table acting as the linkage map.
  - Columns: `Rule ID`, `Human Readable Description`, `File Name`, `Line Range`, `SME Review Status`.
  - **Workflow Definition:** Documenting the sampling strategy and review cycles necessary for baseline approval.

## Milestone Plan

### V1: Proof of Concept Slice
- **Scope:** Complete analysis of [ESDRV212](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/ESDRV212) (Driver) and lines 1-1000 of [ESCAL212](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/ESCAL212) (Initialization & Validation).
- Produce the 4 deliverables for this slice to establish formats and templates.
- **Verification:** USER review and approval of the formats, schema, and UML representations.

### V2: Expanded Slice
- **Scope:** Complete analysis of the remainder of [ESCAL212](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/ESCAL212) (Core Calculations like Outlier, Age Factors, BMI, QIP).
- Expand the 4 deliverables using the V1 baselines.
- Update test harnesses and run regression validations against the growing JSON rule set.

### V3: Complete Set
- **Scope:** Sweeping the remaining programs ([BASECBSA](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/BASECBSA), [WAGECPY](file:///Users/rohitkelapure/Downloads/ESRDCY212MainframePricerReleaseFiles/WAGECPY), legacy `ESCAL` modules if deemed necessary for the modern baseline).
- Finalize all documentation.
- **Verification:** Comprehensive acceptance testing executing the python test harness against all gathered rules.

## User Review Required
- Please advise if the proposed JSON Schema approach and Python-based Test Harness for the "Machine-Readable Rule Artifacts" aligns with your expectations, or if you prefer a different format (e.g., Drools DRL, YAML).
- Please confirm if using standard Mermaid.js sequence/component diagrams within Markdown satisfies the requirement for "UML Asciidraw diagrams", or if you require specifically AsciiDoc syntax (`.adoc`).
